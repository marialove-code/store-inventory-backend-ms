server:
  port: 8082
  servlet:
    context-path: /api

spring:
  application:
    name: inventory-service

# P0 不连数据库；后续阶段再配置 datasource
logging:
  level:
    root: INFO
    com.inventory.stock: INFO
