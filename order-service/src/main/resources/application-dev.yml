# order-service · 本地开发
# profiles=dev 时会加载 concurrency 压测 Controller（@Profile("dev")）
spring:
  datasource:
    password: ${DB_PWD}
  cloud:
    sentinel:
      # 本地连 Dashboard 时保持 false，避免心跳过早
      eager: false

logging:
  level:
    com.inventory: DEBUG
    com.alibaba.cloud.nacos: INFO
