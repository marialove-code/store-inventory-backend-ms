# 微服务拆分 · 起步与阶段记录

## 目录对照

> 当前发布标签：**v3.0.0**（业务拆分完成）

| 用途 | 路径 | 端口 |
|------|------|------|
| 家里在用 · 单体 | `store-inventory-backend` | **8080** |
| 学习 · 微服务 | `store-inventory-backend-ms` | **8081** / 8082 / 8083 / 8084 |

## 目标形态（已确认）

- **platform-service**：认证 + 商品 + 系统 + 看板 + 监控 + 门店（已建）  
- **inventory-service**：库存  
- **order-service**：订单  
- **ai-service**：AI  
- 先拆清业务，**再**引入 Nacos / Gateway  

## P0 已完成

多模块空壳 + ping。

## P1 已完成（2026-07-13）

- 迁入 `StockService` 全能力 + `inventory_stock` / `inventory_flow`  
- 库存列表 / 改预警（无权限注解）  
- 内部命令 API：`/inventory/internal/**`（lock / unlock / decrease-flow / increase / usable）  
- 连同一 PostgreSQL；**未**引入 Nacos；**未**迁入库/出库单完整业务  

### 验证

```powershell
$env:DB_PWD="你的库密码"
cd inventory-service
mvn spring-boot:run

# GET  http://localhost:8082/api/inventory/ping
# GET  http://localhost:8082/api/inventory/stock/list?pageNum=1&pageSize=10
# GET  http://localhost:8082/api/inventory/internal/usable?goodsId=...
# POST http://localhost:8082/api/inventory/internal/lock
#      body: {"goodsId":...,"qty":1,"bizNo":"TEST001"}
```

## P2 已完成（2026-07-13）

- 从单体迁入正式订单业务：`orderinfo` / `orderdelivery` / `orderrefund`（**不含** `concurrency` 压测包）
- `inventory-common` 补充：`OrderStatusEnum`、`RefundStatusEnum`、`OrderNoGenerator`、`OrderPrefix`
- 订单服务 **禁止** 依赖 `StockService` / `InventoryStockMapper`；通过 `InventoryStockClient`（RestTemplate）调  
  `http://localhost:8082/api/inventory/internal/**`
- 配置：`inventory.service.base-url`，端口 8083，连同一 PostgreSQL
- Controller 去掉 `@RequiresPerm`

### 编译

```powershell
cd E:\Projects\store-inventory-backend-ms
# 使用本地 Maven（按本机路径调整）
C:\Users\95349\.m2\wrapper\dists\apache-maven-3.9.11-bin\6mqf5t809d9geo83kj4ttckcbc\apache-maven-3.9.11\bin\mvn.cmd `
  -pl order-service -am clean package -DskipTests
```

### 启动验证

```powershell
$env:DB_PWD="你的库密码"

# 终端 1：先启库存
cd E:\Projects\store-inventory-backend-ms\inventory-service
mvn spring-boot:run

# 终端 2：再启订单
cd E:\Projects\store-inventory-backend-ms\order-service
mvn spring-boot:run

# 探活
# GET  http://localhost:8083/api/order/ping
# GET  http://localhost:8083/api/order/info/list?pageNum=1&pageSize=10
# POST http://localhost:8083/api/order/info/add
#      body: {"userName":"测试","goodsId":...,"goodsName":"...","costPrice":1,"salePrice":2,"buyQty":1}
```

### 事务风险（本阶段接受）

本地 `@Transactional` **管不到** 远程库存 HTTP。建单采用「先 lock 再 save + save 失败尝试 unlock」；发货/退款仍存在「库存已变、本地未提交」窗口，后续用补偿/对账收敛。

## P3 已完成（2026-07-13）

补全库存菜单剩余 4 项（去掉 `@RequiresPerm`；操作人固定 `system`）：

| 菜单 | 路径前缀 | 说明 |
|------|----------|------|
| 入库管理 | `/inventory/stockin` | 列表 / 详情 / 新增（写单 + increaseStockFlow） |
| 出库管理 | `/inventory/stockout` | 列表 / 详情 / 新增（可用库存校验 + decreaseStock） |
| 库存流水 | `/inventory/flow` | 列表 / 详情 / 导出占位 |
| 库存预警 | `/inventory/warn` | 列表（stock &lt; stock_warn）/ 详情 / 改阈值 |

MapperScan 已追加 `stockin.mapper`、`stockout.mapper`。

### 验证（库存服务 8082）

```text
GET  /api/inventory/stockin/list?pageNum=1&pageSize=10
POST /api/inventory/stockin
     body: {"goodsId":"...","goodsName":"...","receiptQty":1}
GET  /api/inventory/stockout/list?pageNum=1&pageSize=10
POST /api/inventory/stockout
GET  /api/inventory/flow/list?pageNum=1&pageSize=10
GET  /api/inventory/warn/list?pageNum=1&pageSize=10
PUT  /api/inventory/warn/{id}/stockWarn
     body: {"stockWarn":10}
```

## P4a 已完成（2026-07-13）

新建独立模块 **`ai-service`（8084）**，从单体迁入全部 AI 能力：

| 能力 | 路径 | 数据来源 |
|------|------|----------|
| 客服 | `POST /ai/chat` | 仅 LLM，会话 memory |
| 智搜 | `POST /ai/product/parse` | 仅 LLM |
| SQL | `POST /ai/sql/query` | JdbcTemplate + 表白名单 |
| 运维 | `GET /ai/ops/analyze` | `sys_operation_log` Mapper |
| 补货 | `GET /ai/inventory/replenish` | `inventory_stock` Mapper |
| 看板洞察/预测 | `/ai/dashboard/**` | 精简 `DashboardIndexService`（JdbcTemplate + 库存聚合） |

适配：去掉 `@RateLimit` / Swagger；暂不鉴权；未迁 Redis 会话；看板不依赖 Redis 在线人数。

### 启动

```powershell
$env:DB_PWD="你的库密码"
$env:DASHSCOPE_API_KEY="可选"
cd E:\Projects\store-inventory-backend-ms\ai-service
mvn spring-boot:run

# GET http://localhost:8084/api/ai/ping
```

## P4 已完成（2026-07-13）

新建 **`platform-service`（8081）**，从单体**照搬**：

- `framework`（Security / JWT / RequiresPerm / RateLimit / OperationLog / ApiMonitor）
- `config`（Redis / Web / Mybatis / Jackson / Security）
- `modules`：auth、goods、system、dashboard、monitor、own、shop

### 跨服务（公共模块 + Client）

| 项 | 说明 |
|----|------|
| `inventory-common` | 完整 `ResultCode`、`RedisConstants`、`StockInitRequest` 等 |
| `POST /inventory/internal/init-stock` | 平台新增商品后初始化库存（幂等） |
| `platform.client.InventoryStockClient` | RestTemplate 调 init-stock / usable |
| 看板 | 暂同库只读 Order/Stock Mapper（已标技术债） |

保留鉴权体系（与 inventory/order/ai「暂不鉴权」不同）。

### 启动

```powershell
$env:DB_PWD="你的库密码"
$env:REDIS_PWD="你的 Redis 密码"
# 建议先启库存 8082，再启平台 8081
cd E:\Projects\store-inventory-backend-ms\platform-service
mvn spring-boot:run

# GET  http://localhost:8081/api/platform/ping
# POST http://localhost:8081/api/auth/login
```

## 下一阶段建议

**P5+**：Feign → Nacos → Gateway；看板订单统计改为 HTTP；分布式事务/补偿完善。
