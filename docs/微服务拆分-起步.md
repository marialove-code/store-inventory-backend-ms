# 微服务拆分 · 起步说明（与单体隔离）

## 目录对照

| 用途 | 路径 | Git | 端口（计划） |
|------|------|-----|----------------|
| 家里在用 · 单体 | `e:\Projects\store-inventory-backend` | `dev` / 标签 `v2.0.0` | **8080**（勿改） |
| 家里在用 · 单体前端 | `e:\Projects\store-inventory-front` | `dev` / 标签 `v2.0.0` | Vite 默认 |
| **学习 · 微服务后端** | `e:\Projects\store-inventory-backend-ms` | 分支 `ms/dev` | 多进程：网关/订单/库存… |
| **学习 · 微服务前端** | `e:\Projects\store-inventory-front-ms` | 分支 `ms/dev` | 另开 Vite，指到 MS 后端 |

远程：当前 `monolith-origin` 指向本地单体仓，**尚未配置 Gitee 新远程**，避免误推覆盖单体。

## 已完成（第 0 步）

1. 从 `v2.0.0` 克隆出 `*-ms` 目录  
2. 创建分支 `ms/dev`  
3. README 标明「微服务学习仓」  

## 下一步（第 1 步起）

1. **Cursor 打开 MS 工作区**（不要在单体仓里改微服务代码）  
2. 后端改成多模块 / 多启动类：先拆 **订单服务 + 库存服务**（共用库可接受）  
3. 引入 **Nacos + OpenFeign**；订单通过 Feign 调库存锁扣  
4. 端口示例（可调整）：库存 `8082`、订单 `8083`、（可选）网关 `8081`  
5. 前端-ms 的 `VITE_API_BASE_URL` 指向网关或聚合入口，**不要**指单体 8080  

## 原则

- 单体继续服务家庭使用与 AI 演示  
- 微服务只在 `*-ms` 里演进，用端口隔离  
- 数据库可先共用 `inventory_store`，表先不拆  
