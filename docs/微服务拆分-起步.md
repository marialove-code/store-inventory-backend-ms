# 微服务拆分 · 起步与阶段记录

## 目录对照

| 用途 | 路径 | 端口 |
|------|------|------|
| 家里在用 · 单体 | `store-inventory-backend` | **8080** |
| 学习 · 微服务 | `store-inventory-backend-ms` | 8082 / 8083 |

## P0 已完成（2026-07-13）

- 清空单体拷贝源码，改为 Maven 多模块  
- `inventory-common`：统一 `Result`  
- `inventory-service` / `order-service`：空壳 + `/ping`  
- **未引入**：Nacos、Feign、Gateway、DB、Security  

### 验证

```text
mvn clean install -DskipTests
GET http://localhost:8082/api/inventory/ping
GET http://localhost:8083/api/order/ping
```

## 下一阶段建议

**P1**：向 `inventory-service` 迁入 `StockService` 与库存表访问（连同一 PostgreSQL），先提供锁/解锁/扣减 HTTP 接口。
