<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.inventory.mapper.SysRoleMapper">

    <resultMap id="BaseResultMap" type="com.inventory.entity.SysRole">
            <id property="id" column="id" />
            <result property="roleName" column="role_name" />
            <result property="roleCode" column="role_code" />
            <result property="sort" column="sort" />
            <result property="status" column="status" />
            <result property="remark" column="remark" />
            <result property="createTime" column="create_time" />
            <result property="updateTime" column="update_time" />
            <result property="isDeleted" column="is_deleted" />
    </resultMap>

    <sql id="Base_Column_List">
        id,role_name,role_code,sort,status,remark,
        create_time,update_time,is_deleted
    </sql>

    <!-- ================================ -->
    <!-- 根据用户ID查询角色编码列表 -->
    <!-- ================================ -->
    <select id="listRoleCodesByUserId" resultType="java.lang.String">
        SELECT DISTINCT r.role_code
        FROM sys_user u
                 -- 关联用户-角色中间表
                 JOIN sys_user_role ur ON u.id = ur.user_id
            -- 关联角色表
                 JOIN sys_role r ON ur.role_id = r.id
        WHERE u.id = #{userId}
          -- 过滤已删除角色
          AND r.is_deleted = 0
          -- 过滤禁用角色
          AND r.status = 1
    </select>
</mapper>
